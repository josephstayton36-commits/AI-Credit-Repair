const firebaseConfig = {
// firebase-config.js
// IMPORTANT: Paste YOUR Firebase config object below:

const firebaseConfig = {
  apiKey: "PASTE_YOURS",
  authDomain: "PASTE_YOURS",
  projectId: "PASTE_YOURS",
  appId: "PASTE_YOURS"
  // If your config includes storageBucket, messagingSenderId, etc. include them too.
};

firebase.initializeApp(firebaseConfig);
